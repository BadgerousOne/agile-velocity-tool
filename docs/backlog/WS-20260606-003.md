# Backlog — WS-20260606-003
# Jira and Azure DevOps native sprint sync

**Role:** Product Owner
**Status:** Approved
**Completed:** 2026-06-06

---

## Acceptance Criteria (finalized)

- [ ] AC-1: A "Sync Sprints" button appears in the Jira card when `connected = true`
- [ ] AC-2: Clicking Sync fetches Jira sprints and displays a preview table (name, start, end, committed pts, completed pts, new/duplicate badge)
- [ ] AC-3: Clicking "Import N sprints" dispatches `IMPORT_SYNCED_SPRINTS`; sprints appear in the app state; duplicates are skipped
- [ ] AC-4: A "Sync Sprints" button appears in the Azure DevOps card when `connected = true`
- [ ] AC-5: Clicking Sync fetches ADO iterations and displays the same preview table
- [ ] AC-6: Auth failures surface an inline error message; no console credential leak
- [ ] AC-7: CORS/network failures surface a clear inline error recommending Electron mode
- [ ] AC-8: `npm run test:run` passes (≥ 85 tests green)
- [ ] AC-9: Existing CSV import in Settings is unaffected

---

## Task List

| # | Task | File(s) | Size | AC |
|---|------|---------|------|----|
| T-1 | Create `src/utils/jiraSync.js` — board fetch, sprint fetch, issue fetch, mapper | new file | M | AC-2, AC-3 |
| T-2 | Create `src/utils/azureSync.js` — iterations fetch, workitem fetch, mapper | new file | M | AC-5 |
| T-3 | Create `src/utils/jiraSync.test.js` | new file | S | AC-8 |
| T-4 | Create `src/utils/azureSync.test.js` | new file | S | AC-8 |
| T-5 | Add `IMPORT_SYNCED_SPRINTS` case to `VelocityContext.jsx` reducer | existing file | S | AC-3 |
| T-6 | Update `Integrations.jsx` — add sync button, preview table, error display, loading state | existing file | L | AC-1–7 |
| T-7 | Update `Integrations.test.jsx` — cover sync button render, preview, import dispatch | existing file | S | AC-8 |
| T-8 | Update workstream record and registry to `ENGINEERING → TESTING` after implementation | docs | XS | — |

---

## Sprint Sizing

Total: M + M + S + S + S + L + S + XS ≈ **1–2 days** engineering effort

---

## Risks

- ADO CORS: Microsoft blocks direct browser-to-dev.azure.com requests in some configurations; FR-005 error handling mitigates this, but some ADO users may only get sync working in Electron mode
- Jira story points field name varies by instance (`story_points` vs `customfield_10016`); fallback logic in jiraSync.js covers the common cases
