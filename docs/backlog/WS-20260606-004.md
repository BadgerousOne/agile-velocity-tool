# Backlog — WS-20260606-004
# Align Ollama URL config between Electron main and Settings UI

**Role:** Product Owner
**Status:** Approved
**Completed:** 2026-06-06

---

## Acceptance Criteria (finalized)

- [ ] AC-1: `window.ollamaApi.setUrl(url)` is callable from the renderer; invoking it updates the URL used by the main process watchdog
- [ ] AC-2: `App.jsx` calls `setUrl` on mount with the stored `buddy_ollama_url` (defaulting to `http://localhost:11434`)
- [ ] AC-3: `probePort()` in `ollama.js` uses the configured URL, not a hard-coded constant
- [ ] AC-4: The production CSP `connect-src` includes `http://localhost:11434`
- [ ] AC-5: Existing `onStatus` / `offStatus` IPC surface is unchanged
- [ ] AC-6: In browser mode, the `setUrl` call in `App.jsx` is a no-op (no errors thrown)
- [ ] AC-7: `npm run test:run` remains green; `npm run build` passes

---

## Task List

| # | Task | File(s) | Size |
|---|------|---------|------|
| T-1 | Add `_ollamaBaseUrl` variable and `setOllamaUrl()` export; update `probePort()` | `electron/ollama.js` | XS |
| T-2 | Import `setOllamaUrl`; register `ipcMain.handle('ollama:setUrl')`; fix CSP | `electron/main.js` | XS |
| T-3 | Expose `setUrl` in preload bridge | `electron/preload.js` | XS |
| T-4 | Add mount `useEffect` to send URL to main process | `src/App.jsx` | XS |
| T-5 | Build verification: `npm run build` | — | XS |

---

## Sizing

Total: 5 × XS ≈ **< 1 hour** engineering effort. No new tests needed — the Electron layer has no existing unit tests; the renderer change in `App.jsx` is a one-liner guarded by `?.`.
